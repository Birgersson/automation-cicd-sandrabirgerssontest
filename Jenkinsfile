pipeline {
    agent any
    stages {
        stage('Deploy/ Build App') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        stage('Frontend test') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        stage('Backend test') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
        stage('Perf test') {
            steps {
                sh 'pwd'
                sh 'ls -lart'
            }
        }
    }
}